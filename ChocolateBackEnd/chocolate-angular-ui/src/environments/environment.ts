const apiVersion = '1.0'

export const environment = {
  production: false,
  serverApiUrl: `https://localhost:8085/api/v${apiVersion}/`,
  // serverApiUrl: `http://localhost:8086/api/v${apiVersion}/`,
  authUrl: `https://localhost:8085`,
};
